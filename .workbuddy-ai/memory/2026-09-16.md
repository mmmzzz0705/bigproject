# 2026-09-16

## 大模型切换：qwen3.8-2.4t-a95b → deepseek-v4.1-flash（原生思考模型）

- 需求：换成"能思考"的 deepseek。先 curl 实测确认可用（200，返回 `reasoning_content`）。
- 关键发现（与 qwen3.8 完全不同）：
  - **恒开思考**：传 `enable_thinking=false` 照样返回思考内容，关不掉。
  - **不支持 `thinking_budget`**：传 1024 返回 200 但被忽略（文档参数表为 `-`）。
    可用 `reasoning_effort`（high/max，不支持 low），未启用。
  - 思考 token 计入 `max_tokens`（usage.completion_tokens 含 reasoning_tokens）。
- 代码改动 `backend/app/services/llm.py`：
  - 思考分支统一 `max_tokens = max(LLM_MAX_TOKENS, THINKING_MIN_TOKENS=8192)`
    （原先只在"翻转重试"分支抬，直接开思考会沿用 4096 被思考挤没）。
  - `_thinking_body()` 由 staticmethod 改实例方法：`thinking_budget` 仅对 `qwen*` 下发。
- 配置 6 处 + 两份 README 共 8 处：模型名换 `deepseek-v4.1-flash`、
  `LLM_ENABLE_THINKING=true`、`LLM_THINKING_BUDGET=0`。
  `test_env_sync` 18 passed。
- 验证：`/api/health` → `llm_provider=dashscope:deepseek-v4.1-flash`、`doc_count=853`；
  容器 env 回读正确；标准问法 17~22s / 口语化 11s，答案 400~560 字符、材料卡片正常；
  泛问与未收录事项仍走候选引导；域外正常拒答；日志无"大模型调用失败"（未降级兜底）。
- 全量 `scripts/qa_smoke.py`（33 次 /api/chat，4m02s）：口语化大多能命中具体事项并直接答
  （个体户设立/变更/注销、居住证、社保卡、灵活就业补贴、公积金租房提取、教师资格、
  失业保险、工伤认定、不动产转移登记…）；泛问「怎么办证」「请问怎么办理」「生完孩子能领什么钱」
  走候选引导；域外与未收录（烟草专卖/犬类准养/低保/残疾人证）全部拒答或引导。**0 次降级**。
- 延迟特征：单轮 11~56s（思考模型，最慢一次 56s）。deepseek-v4.1-flash 只支持
  `reasoning_effort`=high/max（不支持 low），**无法靠调参压低延迟**；
  要快只能换非思考模型。前端 axios 超时 300s，不撞限。
