# 工作台（语料自助增删）· 功能与质量说明

面向维护和二次开发的同学。想直接用，看「一、怎么用」就够了；
要改这块代码，请务必先读「三、设计决策」——那几条是被测试钉住的，改坏会直接丢数据。

## 一、怎么用

启动后打开 <http://127.0.0.1:8080>，左侧切换「智能问答 / 工作台」。

- **添加**：上传 txt / md / pdf / docx（≤10MB），或粘贴标题 + 正文。建议填「语料名称」，
  文件名看不出内容时（如 `qz.pdf`）系统会用 PDF 首页的一行字当标题，很难看。
- **移除**：每行「移出知识库」，需要二次确认。
- **看切分**：「看切分」展开这篇被切成的每个片段（序号 / 章节 / 字数 / 正文），
  判断切分是否合理、章节名有没有认错。
- **重新导入**：内置语料移出后源文件仍保留，可一键恢复。

## 二、接口

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/corpus` | GET | 列表：文档数、片段总数、每篇的片段数/归属/是否已入库 |
| `/api/corpus/upload` | POST | multipart：`file`，可选 `title` |
| `/api/corpus/text` | POST | `{ title, content }` |
| `/api/corpus/{doc_id}` | DELETE | 移出知识库 |
| `/api/corpus/{doc_id}/reingest` | POST | 内置语料恢复 |
| `/api/corpus/{doc_id}/chunks` | GET | 切分预览，`?limit=1..200` |

访问控制：`CORPUS_WRITE_TOKEN`（后端）+ `VITE_WORKBENCH_TOKEN`（前端）配对使用，
留空 = 不校验。部署到公网前必须配，否则任何人都能清空知识库。

## 三、设计决策（改之前必读）

1. **上传语料与内置语料分目录**
   `backend/data/uploads/` 可写；`backend/data/corpus/` 在 compose 里是 `:ro` 只读挂载。
   分目录是为了让删除逻辑按「文件在哪」决定能不能删，不用猜。

2. **删除语义按归属区分**
   - 上传语料：向量 + 文件 + 元数据一起删，不可恢复；
   - 内置语料：**只清向量，保留文件与元数据行**（列表显示 `in_index=false`），
     因为 `doc_id` 由文件名哈希而来，反过来推不出文件名 —— 元数据删了就永远无法重新导入。

3. **片段总数用 `doc_stats` 汇总，不用 `store.count()`**
   Milvus 是逻辑删除，`row_count` 仍算已删未 compaction 的行，用它会出现
   「删了但数字没变」。所以 `/api/health` 的 `doc_count` 可能大于
   `/api/corpus` 的 `total_chunks`，**准确值以后者为准**。

4. **同名 = 更新**：`doc_id` 由文件名哈希而来，入库前先清旧片段再写入，不会留重复副本。
   上传文件若与内置语料同名，落盘时自动改名（`xxx_上传.txt`），防止顶替基准语料。

5. **写操作串行**：`corpus._WRITE_LOCK`。「清旧片段 → 写入」之间有几十秒的 Embedding 窗口，
   两个请求交错会留下半新半旧的片段，且极难复现。

6. **归属（origin）不入库**：按文件当前所在目录实时判定，避免给 PostgreSQL 和 SQLite
   各写一遍迁移。

## 四、开发中修掉的真实缺陷（都有测试守着）

| 问题 | 后果 | 修复 |
| --- | --- | --- |
| 删除时把元数据行也删了 | 内置语料源文件还在却永远无法恢复 | 只清向量，保留元数据 |
| 重复入库不清理旧片段 | 重复片段占满 Top-K，检索质量下降 | 入库前 `_delete_existing` |
| 计数用 `store.count()` | 删除后界面数字不变 | 改用 `doc_stats` |
| 上传文件名与内置语料同名 | 覆盖基准语料全部片段，且删后无法恢复 | 落盘前自动改名 |
| 覆盖入库失败 | 旧片段已删、新片段没进，用户只看到「失败」 | 明确报「旧片段已被移除，请重新上传」 |
| PDF 页眉 URL 当标题 | 列表显示一长串网址 | `extract_title` 跳过 URL 行 + 支持指定标题 |
| 上传文件只在容器卷里 | `down -v` 就丢，宿主机还看不见 | compose 显式挂载到宿主机 |
| Nginx 默认 1MB body 限制 | 大文件 413，且到不了后端 | `client_max_body_size 20m` |

## 五、维护提醒

- 新增语料若带来**新的事项名**，同步 `app/services/rag.py::ENTITY_HINTS`，
  否则相关问题会被判成「没说清办什么」而走候选引导。
- **批量增删语料要重标闸门**：相关性分分布会变，跑
  `backend/scripts/calibrate_gate.py` + `probe_gate.py` 复核
  `VECTOR_MIN_SCORE` / `KEYWORD_MIN_SCORE` / `FUSED_MIN_SCORE` / `VAGUE_VECTOR_MAX`。
- `backend/data/uploads/` 是运行期产物，已在 `.gitignore` 排除
  （`data/corpus` 基准语料必须入库，别一起忽略）。
- 提交前跑 `python scripts/quality_gate.py`。

## 六、已知但不修的事

| 项 | 原因 |
| --- | --- |
| 入库同步阻塞（10~60s） | 当前规模可接受；改异步要引入任务状态、轮询、重启丢状态 |
| 部分片段 `section` 为空 | 既有切分结果，改了要重灌 + 重标闸门 |
| 存在 15 字级碎片 | 同上；若要优化方向是切分时合并过短块，需先跑基线再对比 |
| 溯源列表同文档多条 | 那是不同章节（材料清单/办理流程/…），不是重复 |

## 七、测试

`backend/tests/test_corpus.py`（30 例，纯离线：内存向量库 + 临时 SQLite）：
覆盖上传/粘贴/幂等/删除/内置恢复/切分预览/凭证开关/串行化/文件名防穿越/标题推断。
全量：`python scripts/quality_gate.py`。
