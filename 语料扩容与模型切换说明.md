# 语料扩容与 Embedding 模型切换说明（2026-09-15）

## 1. 模型切换：→ `qwen3.7-text-embedding-flash`

| 项 | 旧值 | 新值 |
|---|---|---|
| 模型 | `tongyi-embedding-vision-flash` | `qwen3.7-text-embedding-flash` |
| Provider | `dashscope_native`（多模态原生端点，一次一条） | `dashscope`（OpenAI 兼容，一次批量） |
| 维度 | 768 | **1024**（也支持 768/512/256） |
| 接口 | `/api/v1/services/embeddings/multimodal-embedding/...` | `/compatible-mode/v1/embeddings` |

代码改动 `backend/app/services/embeddings.py`：

- `OpenAICompatEmbedding` 原实现把整篇文档片段**一次性提交**，而 DashScope 文本向量
  **单请求上限 20 条** → 新增 `BATCH_SIZE=10` 分片 + 线程池并发 + 指数退避重试。
- 超时从 `LLM_TIMEOUT`(300s) 改为 `EMBED_TIMEOUT`(30s)，避免一次挂起阻塞请求线程。
- `dimensions` 参数不被模型支持时自动去掉重试（防止 768/512 维切换时直接失败）。

同步改动：根 `.env`、`backend/.env`、`docker-compose.yml` 三处。

## 2. 语料：12 篇 → 32 篇（新增 GD12~GD31）

新增 20 项广东政务服务事项：

| 编号 | 事项 | 编号 | 事项 |
|---|---|---|---|
| GD12 | 生育津贴申领 | GD22 | 居民身份证换领补领 |
| GD13 | 异地就医备案 | GD23 | 普通护照首次申领 |
| GD14 | 失业保险金申领 | GD24 | 往来港澳通行证及签注申领 |
| GD15 | 企业职工基本养老保险关系转移接续 | GD25 | 机动车驾驶证期满换证 |
| GD16 | 工伤认定 | GD26 | 机动车注册登记 |
| GD17 | 住房公积金租房提取 | GD27 | 食品经营许可证核发 |
| GD18 | 个人住房公积金贷款审批 | GD28 | 有限责任公司设立登记 |
| GD19 | 内地居民结婚登记 | GD29 | 公共场所卫生许可 |
| GD20 | 出生登记（新生儿入户） | GD30 | 教师资格认定 |
| GD21 | 人才引进入户 | GD31 | 公共租赁住房保障申请 |

格式与现有 PDF 抽取语料保持一致（裸行章节名 + 材料表结构），确保
`material._extract_rows` 能正确解析材料清单、`split_sections` 能切出标准章节。

## 3. 重新入库

```bash
docker-compose -f docker-compose.yml up -d --build --force-recreate backend
docker exec gov-backend python scripts/ingest.py --dir /app/data/corpus --clear
```

结果：**32 篇 / 841 片段 / 1024 维**（耗时约 7 分钟）。
旧集合 768 维与配置不符，Milvus 自动删集合重建。`/api/health` 的 `doc_count` 应为 841。

## 4. 阈值重标定（换模型必须做）

实测分布（841 片段）：

| 类别 | 向量分 |
|---|---|
| 纯域外 | 0.335 ~ 0.356 |
| 像政务但未收录 | 0.501 ~ 0.739 |
| 口语化泛问 | 0.418 ~ 0.838 |
| 标准问法 | 0.665 ~ 0.973 |

最终值（`VECTOR_MIN_SCORE=0.45`、`KEYWORD_MIN_SCORE=44.3`、`FUSED_MIN_SCORE=0.62`、
`VAGUE_VECTOR_MAX=0.60`）。

标定脚本推荐的是 0.752（联合错分 0），但 0.752 会把口语化泛问全部挡在门外
（泛问关键词分≈0，只能靠向量分过闸）。取 0.45 让泛问放行，再由
`VAGUE_VECTOR_MAX=0.60` 兜底走候选引导，不硬答具体事项。

同时更新了 `calibrate_gate.py` 的样本集：IN_DOMAIN 扩到 31 条，
NEAR_MISS 换成真正未收录的事项（外地车牌转入 / 离婚登记 / 烟草专卖 / 施工许可 / 医师注册 / 企业年金）。

## 5. 顺带修掉的坑

`rag.py` 的 `ENTITY_HINTS`（业务实体词表）没有跟语料同步扩充，导致
"首次申领普通护照需要什么材料？" 这类**明确问法**、Top-1 也命中了正确文档，
却被 `_is_vague` 判成"没说清办什么"而退化成候选引导。已补充
护照 / 港澳 / 通行证 / 教师资格 / 租赁住房 / 驾驶证 / 机动车 / 异地就医 / 入户 / 人才引进 等词。

> 以后再扩语料，两处必须同步改：`ENTITY_HINTS` 和闸门阈值。

## 6. 配置一致性（换模型最容易漏改的地方）

`tests/test_env_sync.py` 强制 6 份配置的关键键完全一致，换模型后必须全部同步：

`backend/.env`、`backend/.env.example`、`.env`、`.env.docker.example`、
`docker-compose.yml`、`docker-compose.host.yml`

被校验的键：`VECTOR_DIM` / `DASHSCOPE_EMBED_MODEL` / `VECTOR_MIN_SCORE` /
`KEYWORD_MIN_SCORE` / `DASHSCOPE_MODEL`。同文件还有
`test_vector_dim_matches_embedding_model`（模型↔维度配对表，已加入
`qwen3.7-text-embedding-flash: 1024`）。

另外把 `backend/.env` 的本地开发指向从已弃用的宿主旧栈（PG 5433 / Milvus 19530）
改为完整栈（5432 / 19531），`check_env.py` 现在 5/5 通过。

## 7. 验证结果

| 问题 | 结果 |
|---|---|
| 生育津贴怎么申领？ | 直接作答，溯源《生育津贴申领》 |
| 首次申领普通护照需要什么材料？ | 直接作答，材料清单完整 |
| 教师资格认定需要什么材料？ | 直接作答，7 项材料 |
| 公共租赁住房怎么申请？ | 直接作答 |
| 异地就医备案 / 失业保险金 | 直接作答 |
| 我想开个小店，要办啥？ | 候选引导（个体工商户设立 / 有限责任公司设立 / 变更登记） |
| 怎么办证 | 候选引导 |
| 今天天气怎么样？ / 推荐一部好看的电影 | 拒答（不编造） |

### 回归评测 `scripts/eval_rag.py`

**34 / 34 全部通过**（耗时 251s），覆盖五组：

- 域内基础 18 条：命中正确文档 + 给出材料清单，含新增的生育津贴 / 异地就医备案 /
  公积金租房提取 / 结婚登记 / 普通护照。
- 挑战·易混淆 3 条：「不想开了」→ 注销登记，「改名」→ 变更登记，「第一次申领」→ 申领居住证。
- 挑战·数值精确 4 条：承诺办结时限（1 / 5 / 30 / 20 工作日）全部答对。
- 挑战·领域内未收录 4 条：烟草专卖 / 犬类准养证 / 低保 / 残疾人证 —— 全部拒答或泛问引导，不编造。
- 域外 5 条：全部被闸门拦截，未进入生成。

### 其他

- `pytest`：**124 passed**（`test_env_sync` 的 `CRITICAL` 从 5 个键扩到 7 个键，
  把 `FUSED_MIN_SCORE` / `VAGUE_VECTOR_MAX` 也钉死 —— 这两个此前漏管，
  出现过 `backend/.env` 写 0.50、compose 默认 0.60 各跑各的）。
- `check_env.py --skip-llm`：**5/5 通过**（doc_meta=32、841 片段、dim=1024）。
- 材料卡抽取：护照 3 条 / 教师资格 7 条 / 工伤认定 4 条 / 食品经营许可 6 条 / 驾驶证换证 5 条。

### 踩坑：标定脚本的调试值被容器吃进去了

调参时 root `.env` 被写成 `VECTOR_MIN_SCORE=2.0` / `VAGUE_VECTOR_MAX=0.0`
（等于关掉向量通道、关掉泛问引导），`--force-recreate` 后容器照单全收，
而 `/api/health` 一切正常、问答也没报错 —— 典型的静默失败。

- 查法：`docker exec gov-backend env | grep -E "SCORE|VAGUE"`
- 已恢复 0.45 / 0.60；`test_env_sync` 现在把这两个键也纳入 `CRITICAL`，
  跨文件不一致会直接测试红。

## 8. 耐久性验证

```bash
docker-compose -f docker-compose.yml down   # 不带 -v，保留数据卷
docker-compose -f docker-compose.yml up -d
```

21 秒起完，6 个容器全部 healthy，`/api/health` 仍为 `doc_count=841`
（Milvus 集合正常加载，无需重灌）。重启后问答复测：

- 「生育津贴怎么申领？」→ 溯源《生育津贴申领》，材料卡 5 条；
- 「今天天气怎么样？」→ 闸门拦截，拒答。

## 9. 后续补录：GD32《社会保障卡申领》（32 → 33 篇 / 841 → 853 片段）

起因：群众问「社保卡怎么办理？」系统答"知识库暂无该业务相关政策"。
诊断确认**不是闸门或 LLM 故障** —— 检索 Top-1 是《生育津贴申领》（vec 0.598），
语义相邻但不是同一事项，LLM 拒答属于防幻觉设计生效。缺口在语料本身。

### 抓取与整理

```bash
# 广东政务服务网办事指南 PDF 端点（guide 页是 SPA，静态抓不到，走这个旁路）
curl -L -A "Mozilla/5.0" -o gd32.pdf \
  "https://static.gdzwfw.gov.cn/portal/wk-pdf?code=1144098158137993544442111130002&template=guide-v2"
# 部分 code 会返回 500 {"code":"25038","message":"wkhtmltopdf程序出错！"}，换一个地市的 code 重试即可
```

`pypdf` 提取后手工整理成与 GD12~GD31 同构的 txt（PDF 表格会把"承诺办结时限"拆成
"承诺办结时/限/1"三行，必须人工归并）。来源：高州市人力资源和社会保障局，事项版本 15。

### 动作清单

1. 落盘 `backend/data/corpus/GD32_社会保障卡申领_广东政务网.txt`；
   「常见问题 + 温馨提示」两节 GD12 起每篇都有，新语料必须补（会抬高域外关键词分）。
2. `rag.py::ENTITY_HINTS` 补 `社保卡 / 社会保障卡 / 一卡通 / 制卡 / 领卡`
   （不补"卡"这种通用后缀 —— 同"许可证"的教训）。
3. 重灌：`docker exec gov-backend python scripts/ingest.py --dir /app/data/corpus --clear`（6 分 10 秒）。
4. `--build backend`，`/api/health` 的 `doc_count` 应为 **853**。
5. 复核：`docker cp backend/scripts/probe_gate.py gov-backend:/app/scripts/ && docker exec gov-backend python scripts/probe_gate.py`

### 复测结果（853 片段）—— 阈值未调整

| 类别 | 向量分 |
|---|---|
| 纯域外 | 0.331 ~ 0.353 |
| 像政务但未收录 | 0.500 ~ 0.633 |
| 口语化泛问 | 0.428 ~ 0.836 |
| 标准问法 | 0.709 ~ 0.968（社保卡 0.964 / 社保卡办理材料 0.709） |

域外最高 0.353 < 0.45 < 口语化最低 0.428 上方，仍有间隔，`VECTOR_MIN_SCORE` 保持 0.45。

---

## 10. 生成模型切换：→ `deepseek-v4.1-flash`（2026-09-16）

| 项 | 旧值 | 新值 |
|---|---|---|
| 模型 | `qwen3.8-2.4t-a95b` | `deepseek-v4.1-flash` |
| 思考链 | 服务端锁死 `enable_thinking=True`（传 False 报 400） | **原生恒开**，传 False 也照常返回 `reasoning_content` |
| `thinking_budget` | 支持（限制思考 token，140s+ → 22~30s） | **不支持**（传了返回 200 但被忽略） |
| `LLM_ENABLE_THINKING` | `false` | `true` |
| `LLM_THINKING_BUDGET` | `1024` | `0` |

### 服务端实测（curl 直打 `/compatible-mode/v1/chat/completions`）

- `extra_body={"enable_thinking": true}` → 200，`usage.completion_tokens_details`
  含 `reasoning_tokens: 41`，**思考 token 计入 max_tokens**。
- `extra_body={"enable_thinking": false}` → 仍返回 `reasoning_content`。→ 关不掉思考。
- 带 `thinking_budget: 1024` → 200，但思考长度不受控（参数被忽略）。

### 代码改动 `backend/app/services/llm.py`

1. 思考分支统一抬输出预算：
   `max_tokens = max(LLM_MAX_TOKENS, THINKING_MIN_TOKENS=8192)`。
   原实现只在「传 False 报 400 后翻转重试」分支抬升 —— 直接显式开启思考时仍用 4096，
   思考 token 会把正式答案挤没。
2. `_thinking_body()` 由 `staticmethod` 改为实例方法：`thinking_budget` **仅对 `qwen*` 下发**，
   避免对 deepseek 产生"限流已生效"的错觉（它根本不认这个参数）。

### 同步点（8 处）

`backend/.env`、`.env`、`backend/.env.example`、`.env.docker.example`、
`docker-compose.yml`、`docker-compose.host.yml`、`README.md`、`backend/README.md`
（前 6 处由 `tests/test_env_sync.py` 钉死，改漏直接测试红）。

### 验收

| 项 | 结果 |
|---|---|
| `tests/test_env_sync.py` | 18 passed |
| 容器 env 回读 | `DASHSCOPE_MODEL=deepseek-v4.1-flash`、`LLM_ENABLE_THINKING=true`、`LLM_THINKING_BUDGET=0` |
| `/api/health` | `llm_provider=dashscope:deepseek-v4.1-flash`、`doc_count=853`、`vector_store=milvus` |
| 标准问法（个体工商户材料 / 居住证） | 17~22s，答案 408~533 字符，材料卡片正常 |
| 口语化「我想开个小店，要办啥？」 | 11s，命中《个体工商户设立登记》，558 字符 |
| 泛问「怎么办证」/ 未收录「烟草专卖零售许可证」 | 走候选引导，未硬答 |
| 域外「今天天气怎么样？」 | 拒答（0.37s） |
| `docker logs gov-backend \| grep 大模型调用失败` | 为空（未降级抽取式兜底） |

**未重灌向量库、未改 `VECTOR_DIM`、未重标闸门** —— 这三项只跟 Embedding 模型绑定，
换生成模型不影响检索侧。
