# 政明白（GovRAG）· 可行性报告

> 系统全称：政务智能问答与办事引导系统
> 编制日期：2026-09-13
> 依据：本机完整实测（Docker 部署 + 宿主 PostgreSQL / Milvus + 真实 DashScope 接口）
> 所有数字均为实测值；凡属推算均已标明。

---

## 一、结论摘要

| 维度 | 结论 |
| --- | --- |
| **技术可行性** | ✅ **可行**。核心功能已实现并完成端到端验证，回归评测 29/29 通过 |
| **功能达标度** | ✅ 域内问答命中正确事项、给出材料清单/流程/时限/部门；域外与未收录事项拒答不编造 |
| **工程质量** | ✅ 单元测试 120 项全绿（112 项纯离线），配置一致性、向量维度等易错点均有自动化校验 |
| **性能** | ⚠️ 单轮 7~17 秒（取决于是否走生成）；被闸门拦截时 0.3~0.4 秒 |
| **经济可行性** | ✅ 单轮成本极低（见第五章模型）；主要成本是模型调用，与查询量线性相关 |
| **运维可行性** | ✅ 两套 Docker 编排，一键启停；全部外部依赖缺失时自动降级，不影响启动 |
| **主要风险** | 语料覆盖面不足（当前 32 个事项）、大模型输出波动、API 额度依赖 |

**总体结论**：**建议通过可行性论证，进入小范围试点阶段。**
系统已不是概念验证，而是可运行的完整原型；但**语料规模是当前最大约束**，
在扩到区县级政务事项全量（通常数百项）前，不建议直接面向公众全量开放。

---

## 二、项目概述

### 2.1 要解决的问题

群众在办理政务事项前，常有「要什么材料、去哪办、几天办好」的咨询需求。传统方式依赖
窗口人工答复或翻阅办事指南，存在：指南文档冗长（单篇动辄数万字）、事项之间高度相似
（设立/变更/注销登记）容易混淆、人工答复口径不一致等问题。

### 2.2 系统定位

**严格依据已入库政务文档作答的检索增强问答系统（RAG）**，核心约束是**不编造**：

- 有依据 → 给出结论 + 材料清单 + 流程 + 部门 + 时限 + 收费 + 溯源
- 无依据 → 明确回复「知识库暂无该业务相关政策」并引导至线下窗口 / 12345

这一点在政务场景下是**硬要求**：宁可拒答，也不能给出错误的材料清单导致群众白跑。

### 2.3 已实现功能

- 政务事项问答，输出结构化办事材料清单（必备/可选材料、流程步骤、温馨提示）
- 答案溯源（命中文档、章节、相似度评分）
- 多轮会话（历史写入 PostgreSQL、上下文裁剪、会话切换与历史回放）
- 相关性闸门：域外/未收录问题在检索阶段即拦截，不进入生成
- 两套部署形态（完整栈 / 复用宿主服务），以及本地开发热更新模式
- 前端不可用时自动降级为本地演示数据

---

## 三、技术方案

### 3.1 技术栈

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 前端 | Vue 3 + Vite + Nginx | 容器化静态部署，`/api` 反向代理 |
| 后端 | FastAPI + Uvicorn | 异步，自带 `/docs` 接口文档 |
| 关系库 | PostgreSQL 16 | 会话、历史、文档元信息 |
| 向量库 | Milvus 2.x | 生产级向量检索，支持 ANN |
| Embedding | 通义 `qwen3.7-text-embedding-flash` | 1024 维（OpenAI 兼容模式，单请求上限 20 条） |
| 大模型 | 通义 `qwen3.8-flash` | DashScope 兼容模式（OpenAI 协议） |
| 编排 | Docker Compose | 全栈编排 `docker-compose.yml`（PG + Milvus + MinIO + etcd + 后端 + 前端） |

### 3.2 检索链路

```
用户提问
  → expand_query（口语 → 政务术语归一）
  → 混合检索（Milvus 向量 ANN + BM25 关键词，融合加权）
  → _rerank（意图感知重排：材料/流程/时限意图加权，依据类降权）
  → 相关性闸门（向量 ≥0.45 通过；或 关键词 ≥44.3 且融合分 ≥0.62）
      ├─ 未通过 → 直接返回拒答话术（0.3s，不调大模型）
  → 泛问引导（未说清办哪个事项时列出候选事项让用户确认，既不拒答也不硬答）
      └─ 通过    → enrich_sections（定向补全结构化章节：材料/流程/时限/部门）
                 → 构造上下文（≤3000 字）→ 大模型生成 → 溯源 + 材料清单
```

**闸门设计要点**：两个通道是「或」关系，且阈值必须**联合标定**。
若各自独立标定，会低估放行率——实测中联合标定 0 条错分，独立标定关键词通道会错分 2 条。

---

## 四、技术可行性论证（实测数据）

### 4.1 功能达标情况

回归评测 `scripts/eval_rag.py`，34 例，**34/34 通过**，总耗时 251 秒。

| 分组 | 用例 | 结果 |
| --- | --- | --- |
| 域内：命中正确文档 + 给出材料清单 + 不得拒答 | 18 | 18 / 18 |
| 挑战·易混淆事项（设立/变更/注销）首选文档必须正确 | 3 | 3 / 3 |
| 挑战·数值精确（承诺办结时限） | 4 | 4 / 4 |
| 挑战·领域内未收录（烟草专卖/犬类准养/低保/残疾人证）必须拒答 | 4 | 4 / 4 |
| 域外：必须拒答，不得编造 | 5 | 5 / 5 |

**拒答能力是本项目重点验证项**：9 条拒答用例全部正确拦截，
其中领域内未收录的 4 条（烟草专卖零售许可证、犬类准养证、低保、残疾人证）属
「像政务但没收录」，是最容易被大模型编造的场景。
（注：早期用例里的护照、公租房、港澳通行证、外地车牌已随语料扩容变为**已收录**，
该组用例已换成上述真正未收录的事项。）

### 4.2 知识库现状

| 指标 | 数值 |
| --- | --- |
| 语料文档 | 33 篇广东政务办事指南 |
| 原文总量 | 约 257,500 字（含 2026-09-15 新增的 GD32《社会保障卡申领》） |
| 向量片段 | 853（1024 维） |
| 单篇字数 | 1,928 ~ 45,082 字 |

片段分布（前 3 篇占 39%，扩容 20 篇后比原先的 57% 均衡了不少；
新增的 20 篇每篇 13~14 片段，属同一量级的规范化办事指南）：

```
122  城乡居民养老保险参保登记
112  不动产转移登记
 95  房屋所有权首次登记
 42  个体工商户设立登记
 41  个体工商户注销登记
 35  灵活就业社保补贴申领
 35  个体工商户变更登记
 26  一次性创业资助申领
 22  居住证业务申办（补换领签注）
 20  签注居住证
 19  申领居住证
 14  生育津贴申领
 13  普通护照首次申领 / 失业保险金申领 / 居民身份证换领补领 / 内地居民结婚登记
 13  机动车驾驶证期满换证 / 食品经营许可证核发 / 人才引进入户 / 出生登记（新生儿入户）
 13  住房公积金租房提取 / 工伤认定 / 教师资格认定 / 往来港澳通行证及签注申领
 13  公共场所卫生许可 / 个人住房公积金贷款审批 / 企业职工基本养老保险关系转移接续
 13  机动车注册登记 / 公共租赁住房保障申请 / 有限责任公司设立登记 / 异地就医备案
 11  广东省高校毕业生就业创业补贴办事指南
```

> **语料扩容后的重标定**：片段数从 585 → 841、维度从 768 → 1024 后，
> 域外问题的关键词分会明显上抬（补「常见问题」章节后最高达 42.856）。
> `KEYWORD_MIN_SCORE` 已由 35.6 上调至 **44.3**，否则「离婚登记」这类
> 蹭到结婚登记语料里「离婚证」问答的查询会穿闸。**换模型或换语料后必须重标闸门。**

### 4.3 性能实测

| 场景 | 耗时 |
| --- | --- |
| 被闸门拦截（不进生成） | 0.3 ~ 0.4 秒 |
| 正常作答（走生成） | 7 ~ 17 秒 |
| 全量回归评测（34 例） | 251 秒（约 7.4 秒/例均值） |

正常作答的耗时主要由大模型生成构成（含思维链）。抽样两次「居住证怎么申领？」
分别为 16.8s / 14.6s。**对政务咨询场景可接受**（群众填表式等待），
但若用于窗口人员实时辅助，建议进一步优化（见第八章）。

### 4.4 工程质量

| 指标 | 数值 |
| --- | --- |
| 单元测试 | **124 项全绿**（116 项纯离线，8 项接口契约用桩替换 RAG） |
| 第三方依赖 | 106 个包 |
| 自动化守卫 | 配置一致性、向量维度防护、示例文件不得含真实密钥、编排文件必须挂载语料 |

几个关键的自动化守卫（都是踩坑后补的）：

- `test_env_sync`：**6 个配置文件 × 7 个核心键**必须一致（换模型时最容易漏改某一处）
- `test_vectorstore_dim`：`VECTOR_DIM` 与实际返回维度不符时丢弃重建，防止「库看着是满的但分数全错」
- `test_no_external_secret_committed`：示例文件里不能出现真实 API Key

### 4.5 健壮性

- 未配置 PostgreSQL / Milvus / 大模型密钥时**自动降级**为 SQLite + 内存向量库 + 抽取式生成，
  任一外部依赖缺失都不影响启动与演示
- 检索侧故障（Embedding 限流 / 向量库不可达）有**独立话术**，
  与服务「知识库里没有」明确区分，避免把故障误报成「没政策」
- 维度不匹配时 Milvus 删集合重建、本地 JSON 库丢弃重建并留 `.bak`

---

## 五、关键技术难点与已解决方案

这一章是本项目**最有价值的工程资产**。以下问题全部是实测暴露、定位、修复并回归验证的，
其中多个属于「**静默失败**」——不报错，但结果是错的。

### 5.1 检索质量的六个坑

| # | 问题 | 后果 | 解决方案 |
| --- | --- | --- | --- |
| 1 | 文档标题撞名 | GD03/GD03b 正文首行都是「申领居住证」，库里两篇同名，「首次申领」与「到期续期」无法区分 | 改为从**文件名**取标题，32 篇全部唯一 |
| 2 | 材料清单章节取错片段 | 该章节动辄上万字，分数最高的常是「填报须知」噪声段（含"原件/复印件"字样所以像材料表，但解析不出条目）→ 必备材料 0 条 | 同章节多片段时，材料类章节优先取真正能解析出材料行的片段 |
| 3 | 结构化字段从不补全 | 触发词只有材料类，问「承诺多少个工作日办结」时完全不执行 → Top-4 全是审批信息却没有时限那一段 | 触发词扩展为 `STRUCTURED_INTENT_KEYS`，候选池同步放大 |
| 4 | 口语够不到闸门 | 群众说「改名字」，语料写「变更登记」，字面零重叠，两通道都过不了闸被当成域外 | `expand_query` 增加「改名/更名 → 变更登记」等口语映射 |
| 5 | **向量库按章节去重，长章节只能召回一段** | 材料清单被切 20~37 段却只能回来 1 段，且是噪声段 → 材料清单 0 条。**内存库碰巧蒙对，上 Milvus 才稳定复现** | 新增 `fetch_section()` 绕开去重；合并**之后**补齐同章节其他材料片段（只补不换） |
| 6 | 文档级排序打平靠顺序决定主文档 | 注销登记与设立登记最高分都是 0.821，注销靠顺序胜出 → 上下文全是注销内容 → 模型拒答 | 引入「佐证分」（该文档召回片段原始分之和，取前 3）做打平裁决 |

> **第 5、6 项共同说明一个道理**：很多问题只在**生产用的向量库**上才暴露。
> 内存库是暴力检索，会碰巧蒙对；Milvus 是 ANN 近似检索，top-K 会差一两条，
> 边界值随之漂移（域外最高关键词分从 30.17 漂到 31.55）。

### 5.2 评测自身的盲区（重要方法论）

早期「域内」「易混淆」两组**只检查命中的文档对不对、材料条数够不够，从不看答案正文**。
于是「检索命中、但模型返回拒答」这种自相矛盾的响应会被判成**通过**——
实测「居住证到期了怎么续期」就是这样混过去的。

补上「答案不得以拒答话术开头」的断言后，**立刻抓出 2 个真问题**。

> 判据用 `startswith` 而非 `in`：答对了主体、末尾补一句「细节未收录」是有用的回答，
> 用 `in` 会误伤。

### 5.3 其他已解决工程问题

- **语料缺口**：「居住证签注」原无办事指南（只有法规引用），拒答是正确行为。
  已从广东政务服务网 `wk-pdf` 端点补入 GD11（20 片段），该问题转为正常作答。
- **新语料带来新噪声**：事项版本 4+ 的指南多出「跨域通办」区块，罗列全省所有市/区/县/镇名字
  （单篇约 1350 字），是典型**关键词磁铁**。`clean_text()` 已加整块跳过（带 400 行兜底上限）。
- **部署踩坑**：`docker-compose` 的变量替换**优先读 shell 环境变量，其次才读 `.env`**，
  导致改了 `.env` 容器里仍是旧 Key（表现为持续 401）。已写入文档并给出排查命令。

---

## 六、经济可行性

### 6.1 成本构成

| 项目 | 说明 |
| --- | --- |
| 模型调用（主要） | 每次问答 1 次 Embedding + 1 次生成；与查询量**线性**相关 |
| 向量库 / 关系库 | Milvus + PostgreSQL，可自建或托管 |
| 应用服务器 | 2 个容器（后端 + 前端 Nginx），资源占用很低 |
| 语料建设 | 主要人力成本，见 6.3 |

### 6.2 单次问答的 token 模型（实测参数）

| 项 | 取值 | 来源 |
| --- | --- | --- |
| 召回片段数 `TOP_K` | 4 | 配置 |
| 章节补全上限 `MAX_CONTEXT_CHARS` | 3000 字 | 配置（约 2000 tokens） |
| 历史轮数 `MAX_HISTORY_TURNS` | 5 | 配置 |
| 生成答案长度 | 200~400 字（约 300 tokens） | Prompt 约束 |
| Embedding 调用 | 每次问答 1 次（仅对问题） | 实现（语料离线向量化，一次性） |

**单次问答量级**：输入约 2.3k tokens（上下文 + 少量历史）+ 输出约 0.3k tokens，
外加 1 次 Embedding。

> ⚠️ **单价请以阿里云百炼官方报价为准**（本报告未查证到确切计费标准，不做推测）。
> 成本公式：`单次成本 = 2.3k × 输入单价 + 0.3k × 输出单价 + 1 × Embedding 单价`。
> 按常见量级估算，单轮成本在**分厘级别**，1 万次问答的成本远低于一名窗口人员单日人力成本。

### 6.3 语料建设成本（需重点评估）

**这是本项目最主要的持续投入。** 当前 33 篇语料的处理链路已跑通并文档化：

```
广东政务服务网 wk-pdf 端点下载 PDF
  → pypdf 提取
  → 复用原管道 clean()（竖排字段还原等）
  → 本项目 clean_text()
  → ingest.py --file 单独导入（不必全量重建）
```

单篇新增约 20 分钟（含人工核对）。**区县级政务事项通常有数百项**，
全量建设需按此规模折算人力。

### 6.4 成本优化空间

- **闸门前置拦截**：域外/未收录问题 0.3 秒返回，**完全不调用大模型**。
  实测 34 例中有 9 例走这条路，约占 26.5%。在真实场景中（闲聊/无关提问占比更高）
  这个比例会更大，显著节省成本。
- 换更小/更便宜的模型：本项目已从 `qwen3.8-max` 切到 `qwen3.8-2.4t-a95b`，功能未回退（回归 34/34）。

---

## 七、运维可行性

### 7.1 部署

| 编排 | 适用场景 |
| --- | --- |
| `docker-compose.yml` | 完整独立环境（连 PostgreSQL、Milvus+etcd+minio 一起起） |
| `docker-compose.host.yml` | 复用宿主机已有的 PostgreSQL / Milvus，只起应用 |

一键启停，实测 `gov-backend` 约 20 秒转 healthy。

### 7.2 可观测性

- `/api/health` 返回数据库类型、向量库类型、片段数、大模型与 Embedding 型号 —— 一眼看清链路状态
- `scripts/check_env.py` 五步自检：解释器 / 配置 / 关系库 / 向量库 / Embedding / 大模型，
  并校验「模型实际返回维度 == 配置维度」
- `scripts/calibrate_gate.py --write` 一键重标闸门阈值（换 Embedding 模型后必跑）

### 7.3 降级能力

任一外部依赖缺失都不影响启动：关系库 → SQLite，向量库 → 内存，大模型 → 抽取式生成。

---

## 八、风险分析与对策

| # | 风险 | 影响 | 对策 | 现状 |
| --- | --- | --- | --- | --- |
| 1 | **语料覆盖不足**（仅 32 个事项） | 群众问到未收录事项只能拒答，体验受损 | 扩充语料；拒答时给出 12345 引导 | ⚠️ 主要约束 |
| 2 | 大模型输出波动 | 材料条数时多时少（实测换模型后 10→2 条） | 已设最低条数断言；抽取式兜底 | 已缓解 |
| 3 | API 额度/密钥依赖 | 额度耗尽即不可用（**实测已发生一次**） | 监控余额；检索故障有独立话术，不误报 | 已有话术，建议加监控告警 |
| 4 | 语料内容过期 | 政策变更导致给出旧流程 | 需建立定期重跑语料的机制 | ⚠️ 未建立 |
| 5 | 单轮 7~17 秒偏慢 | 窗口实时辅助场景受限 | 换更快模型 / 关闭思维链 / 精简上下文 | 已切 flash，可继续优化 |
| 6 | 政务数据合规 | 办事指南多来自公开渠道，相对安全；但会话历史含个人信息 | 会话数据本地存储，不外传；建议评估脱敏与留存策略 | ⚠️ 需合规评审 |
| 7 | 误答导致群众白跑 | 政务场景最严重的问题 | 严格依据语料 + 拒答优先 + 答案溯源 | 已重点设计 |

---

## 九、局限性与后续建议

### 9.1 当前局限性

1. **语料仅 32 个事项**，且分布仍不均衡（前 3 篇占 39.1% 片段）
2. **单轮 1.7~4.3 秒**（跨运行曾观测到 56.6 秒的极端值），不适用于窗口实时辅助
3. **拒答时仍返回溯源列表**（是依据条文而非办事指南），前端展示上略显矛盾
4. **无语料更新机制**，政策变更后需人工重跑
5. **无用户反馈闭环**，无法从「群众觉得答案有没有用」中学习

### 9.2 后续建议（按优先级）

| 优先级 | 建议 | 说明 |
| --- | --- | --- |
| P0 | **扩充语料**至试点范围全量事项 | 已跑通单篇 20 分钟的链路，可批量 |
| P0 | 建立**语料定期更新机制** | 政策变更风险 |
| P1 | 优化**首字延迟** | 关闭思维链 / 更短上下文 / 流式输出 |
| P1 | 补充**用户反馈闭环** | 「答案是否有用」按钮，积累 bad case |
| P2 | 前端**流式输出** | 改善等待体验（当前是整段返回） |
| P2 | 拒答时**清空溯源** | 避免「无政策但有 7 条溯源」的矛盾观感 |
| P2 | 接入**统一的政务事项数据源** | 替代手工下载 PDF |

---

## 十、结论

**本项目通过可行性论证。**

1. **技术上完全可行**：检索增强 + 严格拒答的设计已在真实数据上验证，
   29/29 回归评测通过，120 项单元测试全绿，工程化程度较高（含多项自动化守卫）。

2. **经济上可行**：单次问答成本在分厘量级，且约三成问题由闸门前置拦截不消耗模型调用；
   主要投入是语料建设的人力。

3. **运维上可行**：两套 Docker 编排一键部署，健康检查和依赖自检齐备，外部依赖缺失自动降级。

4. **主要约束是语料规模**，而非技术方案。当前 32 个事项仍不足以支撑全量开放。

**建议：进入小范围试点阶段** —— 选取 1~2 个高频事项领域（如个体工商户登记、居住证业务）
扩充至该领域全量事项，配合真实用户反馈验证后再逐步推广。

---

## 附录：实测环境

| 项 | 值 |
| --- | --- |
| 操作系统 | Windows + Docker Desktop 29.7.2 |
| 后端 | FastAPI（容器 `gov-backend`） |
| 前端 | Vue 3 + Nginx（容器 `gov-frontend`） |
| 关系库 | PostgreSQL 16（容器 `gov-postgres`，宿主 5432） |
| 向量库 | Milvus（容器 `gov-milvus`，宿主 19531），集合 `gov_docs`，853 片段 / 1024 维 |
| 大模型 | `qwen3.8-2.4t-a95b`（DashScope 兼容模式） |
| Embedding | `qwen3.7-text-embedding-flash`（1024 维，OpenAI 兼容模式） |
| 闸门阈值 | 向量 ≥ 0.45，关键词 ≥ 44.3 且融合分 ≥ 0.62，泛问引导 0.50（1024 维模型 / 853 片段上标定，GD32 入库后复测仍成立；换模型或换语料必须重标） |
| Python（本地开发） | 3.12.14（项目虚拟环境 `backend/.venv`） |
